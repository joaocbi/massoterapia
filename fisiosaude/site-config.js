// FisioSaúde — physiotherapy clinic template (blue theme + brand).
// For a dedicated production: create your own Vercel project + Postgres and set apiBaseUrl to that API.
// Marketing cards use these service names; in Admin > Services, add matching lines (Name|Duration|Price).
window.LUXOR_CONFIG = {
  apiBaseUrl: "https://flowterapia.vercel.app",
  brandName: "FisioSaúde",
};
